# robots.txt — theclearstack.com
# Last updated 2026-06-19
# Policy: OPTION 1 — allow ALL compliant search + AI crawlers (citation AND
# training) for maximum visibility and long-term brand recall in AI models.
# IP is protected by NOT publishing the proprietary methodology, not by
# blocking crawlers.
#
# Only the paid Playbook is disallowed. Known abusive crawlers are blocked.
#
# RULE: exactly ONE group per user-agent. Cloudflare "Managed robots.txt"
# must stay OFF so it cannot prepend a conflicting block that disallows the
# AI crawlers (GPTBot, ClaudeBot, etc.) this file intends to allow.

# --- Default: every compliant crawler ---
User-agent: *
Allow: /
Disallow: /playbook/
Disallow: /playbook-download.html

Sitemap: https://theclearstack.com/sitemap.xml

# --- Known abusive crawler: ignores robots.txt; also block at Cloudflare WAF ---
User-agent: Bytespider
Disallow: /
